name: Debian Package + apt repo

on:
  push:
    branches: [master]
  pull_request:

# Least privilege by default: only the publish job below raises this, and only
# for itself. The build jobs run Docker and clone third-party sources, so they
# should never hold a token that can write to Pages.
permissions:
  contents: read

jobs:
  build-deb:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        suite: [trixie, sid]
    container: debian:${{ matrix.suite }}
    steps:
      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential ca-certificates git \
            dpkg-dev debhelper dh-dkms python3

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Trust repo directory
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Set changelog version from git
        run: python3 packaging/deb-version.py --write-changelog

      - name: Build package
        run: dpkg-buildpackage -b -us -uc

      - name: Collect debs
        run: |
          mkdir -p built-debs
          cp ../*.deb built-debs/

      - uses: actions/upload-artifact@v4
        with:
          name: debs-${{ matrix.suite }}
          path: built-debs/

  publish-apt:
    # Scoped to this job alone. A job calling a reusable workflow sets the
    # maximum permissions available to it, so the elevated Pages scopes stay
    # here rather than applying to the whole workflow.
    permissions:
      contents: read
      pages: write
      id-token: write
    # Build on PRs to validate, but never publish from one: that would
    # overwrite the live repository with unreviewed packages.
    if: github.event_name != 'pull_request'
    needs: build-deb
    uses: mithro/apt-repo-action/.github/workflows/publish-apt.yml@main
    with:
      suites: "trixie sid"
      # Architecture: all -- one deb serves every architecture.
      architectures: "all"
      origin: traverse-sensors
      description: "Traverse Ten64 sensor collectors"
      keyring-name: traverse-sensors.gpg
    secrets:
      gpg-private-key: ${{ secrets.APT_GPG_PRIVATE_KEY }}
